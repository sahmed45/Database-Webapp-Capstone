@page
@model KSUWEBPMOCAP.Pages.Profiles.UserProfile.Students.IndexModel

@{
    Layout = "/Pages/Shared/_Layout2.cshtml";
    ViewData["Title"] = "Index";
}

<h1>Students</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].Student_Net_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].Student_LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].Student_FirstdName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].Student_Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Student) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Student_Net_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student_LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student_FirstdName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student_Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Student_ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Student_ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Student_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
